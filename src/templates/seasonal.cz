template SeasonalBouncer from Bouncer {
    tags + BouncerSeasonal, BouncerSeasonal$(year)
    hidden + Deploy
}

template SeasonalGroup from Group {
    seasonalYear = $year
    seasonalStart = $start
    seasonalEnd = $end
}
